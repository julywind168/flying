[package]
name = "flying"
version = "0.1.0"
edition = "2024"
description = "A distributed service framework"
license = "Apache-2.0"

[dependencies]
actix = "0.13.5"
mlua = { version = "0.10", features = ["lua54", "vendored", "async"] }
